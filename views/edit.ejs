<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="/css/style.css">
  <title>Edit Book</title>
</head>
<body>
  <h1>Edit Book</h1>
  <form action="/books/<%= book.id %>/edit" method="POST">
    <input type="text" name="title" value="<%= book.title %>" placeholder="Book Title" required>
    <input type="text" name="author" value="<%= book.author %>" placeholder="Author">
    <textarea name="notes" placeholder="Notes"><%= book.notes %></textarea>
    <input type="date" name="date_read" value="<%= book.date_read ? book.date_read.toISOString().split('T')[0] : '' %>">
    <input type="number" name="rating" value="<%= book.rating %>" placeholder="Rating (1-10)" min="1" max="10">
    <input type="url" name="periplus_link" value="<%= book.periplus_link %>" placeholder="Periplus Link">
    <input type="url" name="amazon_kindle_link" value="<%= book.amazon_kindle_link %>" placeholder="Amazon Kindle Link">
    <button type="submit" class="btn">Save Changes</button>
  </form>
</body>
</html>
